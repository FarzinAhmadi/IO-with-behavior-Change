from setuptools import find_packages, setup

setup(
    name='MfpDataLib',
    packages=find_packages(),
    # version='0.1.0',
    description='Library of functions for processing, analyzing, and plotting MyFitnessPal data',
    # install_requires=[]
)